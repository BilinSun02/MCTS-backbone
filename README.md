# MCTS-backbone
This is an implementation of a fully generic MCTS backbone that supports safe parallel explorations (selections and expansions etc.). Derive the base classes for concrete tasks.

The actual code is mostly written by ChatGPT 5.4 Thinking. The conversation leading up to the code is available at `docs/Initial_implementation_chat.html`. It also contains details like how exactly to write derived classes.
